<!DOCTYPE html>
<html lang="pt-BR" translate="no">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<title>DFS TECH API</title>

<!-- FAVICONS -->
<link rel="icon" type="image/png" sizes="32x32" href="https://files.catbox.moe/w6l8pf.jpg">
<link rel="icon" type="image/png" sizes="16x16" href="https://files.catbox.moe/w6l8pf.jpg">
<link rel="apple-touch-icon" href="https://files.catbox.moe/w6l8pf.jpg">
<link rel="shortcut icon" href="https://files.catbox.moe/w6l8pf.jpg">

<!-- Font Awesome (corrigido com integrity e crossorigin) -->
<link rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
  integrity="sha512-iecdLmaskl7CVkqkXNQ/Zq+U4t8VZlS4zE4n5PQK3jz5nL3l+VgD8VQ1qD5Z2Pq2i0p8xD0pZV0Q5V2z6VYfBw=="
  crossorigin="anonymous" referrerpolicy="no-referrer" />

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
<style>
:root {
  --primary-700: #B00000;
  --primary-600: #C40000;
  --primary-500: #E10600;
  --primary-400: #FF2A1F;
  --primary-300: #FF4D40;
  --primary-200: #FF7A70;
  --primary-100: #FFD2CF;

  --primary: #E10600;
  --secondary: #ffffff;
  --dark: #000000;
  --light: #111111;
  --gray: #cccccc;
  --light-gray: #222222;
  --text: #ffffff;
  --bg: rgba(0,0,0,0.6);
}
*{margin:0;padding:0;box-sizing:border-box;}
html, body {width:100%;min-height:100vh;font-family:'Poppins',sans-serif;color:var(--text);background:var(--bg);overflow-x:hidden;}
a{text-decoration:none;color:inherit;transition:all 0.3s ease;}
.container{width:100%;max-width:1200px;margin:0 auto;padding:0 20px;}
.btn{display:block;width:100%;max-width:280px;margin:10px auto;padding:14px 28px;border-radius:10px;font-weight:600;text-align:center;transition:all 0.3s ease;cursor:pointer;font-size:1.1rem;position:relative;overflow:hidden;}
.btn-primary{background-color:var(--primary);color:var(--secondary);border:2px solid var(--primary);}
.btn-primary:hover{background-color:#C40000;border-color:#C40000;}
.btn-outline{background-color:transparent;color:var(--primary);border:2px solid var(--primary);}
.btn-outline:hover{background-color:var(--primary);color:var(--secondary);}
.main-header{position:fixed;top:0;left:0;width:100%;background-color:rgba(0,0,0,0.9);box-shadow:0 2px 20px rgba(0,0,0,0.5);z-index:1000;padding:15px 0;}
.navbar{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;}
.logo{display:flex;align-items:center;gap:10px;}
.logo img{width:50px;height:50px;}
.logo h1{font-size:1.5rem;font-weight:600;color:var(--primary);}
.nav-menu{display:flex;gap:30px;align-items:center;flex-wrap:wrap;}
.nav-menu a{font-weight:500;color:var(--text);}
.nav-menu a:hover{color:var(--primary);}
.login-btn{margin-left:20px;}
.hero-section{width:100%;padding:150px 20px 50px;position:relative;z-index:1;}
.hero-content{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:20px;position:relative;z-index:2;}
.hero-text{text-align:center;max-width:90%;z-index:2;margin:0 auto;line-height:1.6;}
.hero-text h1{font-size:clamp(2rem,6vw,3rem);font-weight:700;color:var(--primary);margin-bottom:15px;line-height:1.2;}
.lead{font-size:clamp(1rem,4vw,1.2rem);color:var(--gray);margin-bottom:20px;line-height:1.6;}
.cta-text{font-size:clamp(1rem,3vw,1.1rem);font-weight:600;margin-bottom:15px;color:var(--secondary);}
.hero-buttons{display:flex;flex-direction:column;gap:12px;margin-bottom:20px;flex-wrap:wrap;}
.visit-counter{font-size:1rem;font-weight:600;color:var(--secondary);background:var(--light-gray);padding:10px 20px;border-radius:10px;display:inline-flex;align-items:center;gap:8px;box-shadow:0 0 10px rgba(225, 6, 0,0.4);}
.visit-counter i{color:var(--primary);font-size:1.2rem;}
.main-footer{background-color:var(--dark);color:var(--secondary);padding:40px 0 20px;position:relative;z-index:2;}
.footer-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:30px;margin-bottom:30px;}
.footer-title{font-size:1rem;font-weight:600;margin-bottom:15px;color:var(--primary);}
.footer-links a{color:var(--gray);}
.footer-links a:hover{color:var(--secondary);}
.footer-bottom{border-top:1px solid var(--light-gray);text-align:center;padding-top:15px;color:var(--gray);}
#bgCanvas{position:fixed;top:0;left:0;width:100%;height:100%;z-index:-1;pointer-events:none;}
.marquee-footer {position: fixed;bottom: 0;width: 100%;background-color:rgba(0,0,0,0.9);overflow:hidden;white-space:nowrap;box-shadow:0 -2px 10px rgba(225, 6, 0,0.5);z-index:999;}
.marquee-footer p {display:inline-block;padding-left:100%;animation: marquee 15s linear infinite;font-size:1.2rem;font-weight:600;color:var(--primary);}
@keyframes marquee {0% { transform: translateX(0); }100% { transform: translateX(-100%); }}
@media(max-width:768px){.hero-text{max-width:95%;padding:0 10px;}.hero-buttons{gap:8px;}.btn{font-size:1rem;padding:12px 20px;}.lead{font-size:1rem;}.cta-text{font-size:1rem;}}
/* ESTILO DO ALERTA */
#customAlert {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 400px;
  background-color: rgba(0,0,0,0.85);
  color: #fff;
  border-radius: 12px;
  text-align: center;
  z-index: 2000;
  overflow: hidden;
  font-family: 'Poppins', sans-serif;
  animation: fadeIn 0.5s;
}
#customAlert img {
  width: 100%;
  display: block;
}
#customAlert p {
  margin: 10px 0;
  padding: 0 10px;
  font-size: 16px;
}
#customAlert button {
  margin-bottom: 10px;
  padding: 5px 15px;
  border: none;
  border-radius: 6px;
  background-color: #FF2A1F;
  color: #fff;
  cursor: pointer;
  font-size: 14px;
}
@keyframes fadeIn { from {opacity: 0;} to {opacity: 1;} }
@keyframes fadeOut { from {opacity: 1;} to {opacity: 0;} }
</style>
</head>
<body translate="no">

<canvas id="bgCanvas"></canvas>

<header class="main-header">
  <div class="container">
    <nav class="navbar">
      <a href="#" class="logo" translate="no">
        <img src="https://files.catbox.moe/w6l8pf.jpg" alt="DFS TECH API">
        <h1 translate="no">DFS TECH API</h1>
      </a>
      <ul class="nav-menu">
        <li><a href="https://chat.whatsapp.com/DGlSG0mylLxDDBrFgm2UrI"><i class="fa-brands fa-whatsapp"></i> COMUNIDADE</a></li>
        <li><a href="/bots"><i class="fa-solid fa-robot"></i> BOTS E CASES</a></li>
        <li><a href="https://chat.whatsapp.com/LDyG4QEaLlpDTgcoJMbv66"><i class="fa-solid fa-headset"></i> GRUPO DFS TECH</a></li>
        <li><a href="/upload"><i class="fa-solid fa-upload"></i> UPLOADS</a></li>
        <li><a href="/login.html" class="btn btn-outline login-btn"><i class="fa-solid fa-right-to-bracket"></i> LOGIN</a></li>
      </ul>
    </nav>
  </div>
</header>

<section class="hero-section">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text" data-aos="fade-up" data-aos-duration="1000">
        <h1 translate="no">DFS TECH API</h1>
        <p class="lead">Chega de limitações, instabilidades e promessas vazias! Na <b translate="no">DFS TECH API</b> você encontra segurança, velocidade e suporte real.</p>
        <p class="cta-text">Escolha o que você deseja explorar:</p>
        <div class="hero-buttons">
          <a href="/planos" class="btn btn-primary"><i class="fa-solid fa-list"></i> PLANOS API</a>
          <a href="/chat" class="btn btn-outline"><i class="fa-solid fa-robot"></i> INTELIGÊNCIA ARTIFICIAL</a>
          <a href="/dfs-tech-tube" class="btn btn-outline"><i class="fa-solid fa-music"></i> DFS TECH TUBE</a>
          <a href="/docs#painel-puxadas" class="btn btn-outline"><i class="fa-solid fa-database"></i> PAINEL PUXADAS</a>
          <a href="/dfs-tech-hosting" class="btn btn-primary"><i class="fa-solid fa-comment"></i> DFS TECH HOSTING</a>
        </div>
        <div class="visit-counter" id="visitCounter" data-aos="fade-up" data-aos-delay="400">
          <i class="fa-solid fa-eye"></i> Total de visitas: <span id="visitNumber">0</span>
        </div>
      </div>
    </div>
  </div>
</section>

<footer class="main-footer">
  <div class="container">
    <div class="footer-grid">
      <div>
        <div class="logo" translate="no">
          <img src="https://files.catbox.moe/w6l8pf.jpg" alt="DFS TECH API">
          <h1 translate="no">DFS TECH API</h1>
        </div>
        <p>Impulsione o sucesso do seu projeto com nossa API e Hospedagem de alta performance e suporte 24 horas 100 % humanizado</p>
      </div>
      <div class="footer-links">
        <h3 class="footer-title">Links</h3>
        <ul>
          <li><a href="https://whatsapp.com/channel/0029Vb6iKLhDp2QIh64HGk1t"><i class="fa-brands fa-whatsapp"></i> Canal Whatsapp</a></li>
          <li><a href="https://www.youtube.com/@Toshdfs-tech2"><i class="fa-brands fa-youtube"></i> Canal YouTube</a></li>
          <li><a href="/credito"><i class="fa-solid fa-certificate"></i> Créditos</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <p><span translate="no"></span></p>
    </div>
  </div>
</footer>

<div class="marquee-footer">
  <p>OLÁ, SOMOS A DFS TECH API. VOCÊ SABIA QUE AGORA VOCÊS PODEM CONTAR COM A NOSSA HOSPEDAGEM OFICIAL DFS TECH HOSTING NA BIO?</p>
</div>

<!-- ALERTA -->
<div id="customAlert">
  <img src="https://files.catbox.moe/w6l8pf.jpg" alt="Alerta">
  <p>Bem vindos a DFS TECH onde você encontra api fibra e hospedagem veloz e alto desempenho agradecemos pela preferência</p>
  <button onclick="closeAlert()">Fechar</button>
</div>

<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>
document.addEventListener("DOMContentLoaded", () => {
  AOS.init();
  let visits = localStorage.getItem("visits");
  if (!visits) visits = 0;
  visits++;
  localStorage.setItem("visits", visits);
  document.getElementById("visitNumber").textContent = visits;
  const canvas = document.getElementById('bgCanvas');
  const ctx = canvas.getContext('2d');
  const resizeCanvas = () => { canvas.width = window.innerWidth; canvas.height = window.innerHeight; };
  window.addEventListener('resize', resizeCanvas);
  resizeCanvas();
  const particles = [];
  const particleCount = 80;
  for (let i = 0; i < particleCount; i++) {
    particles.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 1.5,
      vy: (Math.random() - 0.5) * 1.5,
      radius: 3 + Math.random() * 3,
      color: `rgba(225, 6, 0,${0.6 + Math.random() * 0.4})`
    });
  }
  function animate() {
    ctx.fillStyle = "rgba(0,0,0,0.6)";
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    for (let p of particles) {
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
      ctx.fillStyle = p.color;
      ctx.fill();
    }
    for (let i = 0; i < particles.length; i++) {
      for (let j = i + 1; j < particles.length; j++) {
        let dx = particles[i].x - particles[j].x;
        let dy = particles[i].y - particles[j].y;
        let dist = Math.sqrt(dx*dx + dy*dy);
        if(dist < 120){
          ctx.beginPath();
          ctx.moveTo(particles[i].x, particles[i].y);
          ctx.lineTo(particles[j].x, particles[j].y);
          ctx.strokeStyle = `rgba(225, 6, 0,0.3)`;
          ctx.stroke();
        }
      }
    }
    for (let p of particles){
      p.x += p.vx;
      p.y += p.vy;
      if(p.x < 0 || p.x > canvas.width) p.vx*=-1;
      if(p.y < 0 || p.y > canvas.height) p.vy*=-1;
    }
    requestAnimationFrame(animate);
  }
  animate();

  // ALERTA
  const alertBox = document.getElementById('customAlert');
  alertBox.style.display = 'block';
  setTimeout(() => {
    alertBox.style.animation = 'fadeOut 0.5s';
    setTimeout(() => { alertBox.style.display = 'none'; }, 500);
  }, 15000); // alterado para 15 segundos
});

function closeAlert(){
  const alertBox = document.getElementById('customAlert');
  alertBox.style.animation = 'fadeOut 0.5s';
  setTimeout(() => { alertBox.style.display = 'none'; }, 500);
}
</script>

</body>
</html>
